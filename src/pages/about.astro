---
import BaseLayout from '../layouts/BaseLayout.astro';
import PageHeader from '../components/PageHeader.astro';
import '../styles/about.css';

const githubUrl = 'https://github.com/s1oopX';
const gmailAddress = 's1oopp1337@gmail.com';

const contactRows = [
  { label: 'GitHub', value: 's1oopX', href: githubUrl },
  { label: 'Gmail', value: gmailAddress, href: `mailto:${gmailAddress}` },
];

const profileRows = [
  { label: 'Owner', value: 's1oop' },
  { label: 'Runtime', value: 'Cloudflare' },
  { label: 'Source', value: 'D1' },
];

const principles = [
  {
    title: '先保证阅读',
    copy: '页面保持安静，正文和入口优先，减少不会长期使用的装饰。',
  },
  {
    title: '再降低维护',
    copy: '公开文章统一进入 D1，GitHub 只维护站点代码、样式和页面结构。',
  },
  {
    title: '最后方便回找',
    copy: '专栏、搜索和更新记录都服务于后续回看，不额外制造内容入口。',
  },
];

const projectRows = [
  { label: 'Shell', value: 'Astro' },
  { label: 'Code', value: 'GitHub' },
  { label: 'Runtime', value: 'Pages' },
  { label: 'Data', value: 'D1' },
];
---

<BaseLayout
  title="关于 | s1oop's Blog"
  description="关于 s1oop's Blog：一个面向长期阅读和轻量维护的个人博客。"
>
  <section class="ui-page archive-page about-page">
    <PageHeader
      eyebrow="about"
      title="关于"
      description="这里保存写作、阅读、设计判断和轻量技术实践。"
    />

    <div class="ui-grid ui-page-body">
      <main class="ui-stack ui-stagger">
        <section class="ui-panel about-summary" aria-labelledby="about-summary-title">
          <div class="ui-panel-header">
            <p class="ui-eyebrow">personal archive</p>
            <h2 id="about-summary-title">长期回看的个人档案。</h2>
          </div>
          <div class="ui-panel-body">
            <p>
              这个博客不按热度排序，也不追逐频繁刷新。它更像一个可以反复打开的个人档案：
              文章、标签、专栏和搜索都只服务一件事，让写过的内容在之后还能被找到、读懂和继续修正。
            </p>
            <div class="about-principle-list" aria-label="维护原则">
              {principles.map((item) => (
                <article>
                  <h3>{item.title}</h3>
                  <p>{item.copy}</p>
                </article>
              ))}
            </div>
          </div>
        </section>
      </main>

      <aside class="ui-sidebar ui-stagger" aria-label="博客说明">
        <section class="ui-panel about-profile">
          <div class="ui-panel-body">
            <div class="about-profile-head">
              <img src="/images/avatar.jpg" alt="" width="128" height="128" loading="eager" decoding="async" />
              <div>
                <p class="ui-eyebrow">s1oop</p>
                <h2>个人博客</h2>
                <p>写作、阅读和实践记录。</p>
              </div>
            </div>
            <dl class="about-meta-list">
              {profileRows.map((item) => (
                <div>
                  <dt>{item.label}</dt>
                  <dd>{item.value}</dd>
                </div>
              ))}
            </dl>
          </div>
        </section>

        <section class="ui-panel">
          <div class="ui-panel-header">
            <h2 class="ui-panel-title">正式联系</h2>
          </div>
          <div class="ui-panel-body about-link-list">
            {contactRows.map((item) => (
              <a href={item.href} target={item.href.startsWith('mailto:') ? undefined : '_blank'} rel={item.href.startsWith('mailto:') ? undefined : 'noreferrer'}>
                <span>{item.label}</span>
                <strong>{item.value}</strong>
              </a>
            ))}
          </div>
        </section>

        <section class="ui-panel">
          <div class="ui-panel-header">
            <h2 class="ui-panel-title">项目指纹</h2>
          </div>
          <div class="ui-panel-body grid gap-4">
            {projectRows.map((item) => (
              <div class="ui-stat-row">
                <span class="ui-stat-label">{item.label}</span>
                <span class="ui-stat-value">{item.value}</span>
              </div>
            ))}
          </div>
        </section>
      </aside>
    </div>
  </section>
</BaseLayout>
